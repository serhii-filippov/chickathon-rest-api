'use strict';

const users = require('../controllers/users');
const bots = require('../controllers/bots');
const logs = require('../controllers/logs');
const battles = require('../controllers/battles');

module.exports = {
    users, bots, logs, battles
}